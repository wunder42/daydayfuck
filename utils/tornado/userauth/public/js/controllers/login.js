// Generated by CoffeeScript 1.6.3
(function() {
  'use strict';
  define([], function() {
    var LoginController;
    LoginController = function($scope, $http, $location) {
      $scope.user = {
        username: '',
        password: ''
      };
      $scope.requestLogin = function() {
        return $http.post('/u/login', {
          username: $scope.user.username,
          password: $scope.user.password
        }).success(function(data, status, headers, config) {
          console.log('success y', data["type"]);
          $scope.user.username = '';
          $scope.user.password = '';
          if (data["type"] === 1) {
            return window.location.href = data.url;
          }
        }).error(function(data, status, headers, config) {
          return console.log('error', data["type"]);
        });
      };
      return $http.get('/u/login').success(function(data, status, headers, config) {
        console.log('success x', data["type"]);
        if (data["type"] === 1) {
          if (data.url === '/#/login') {
            $scope.user.username = '';
            $scope.user.password = '';
            return;
          }
          return window.location.href = data.url;
        }
      });
    };
    return LoginController;
  });

}).call(this);
